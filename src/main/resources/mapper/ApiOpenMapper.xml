<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.api.open.dao.ApiOpenMapper">

    <select id="getJsonWithUrl" resultType="com.api.open.model.ApiJsonModel">
        SELECT * FROM apijson WHERE url = #{url}
    </select>

</mapper>